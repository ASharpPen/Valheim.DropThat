using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Valheim.DropThat.ConfigurationTypes;

namespace Valheim.DropThat.Conditions
{
    public static class ConditionChecker
    {
        /*
        public static bool AreConditionsValid(CharacterDrop characterDrop, DropConfiguration dropConfiguration)
        {
            bool isValid = true;

            //TODO: Check each condition
            var character = characterDrop.gameObject.GetComponent<Character>();

            int level = character.GetLevel())
            
            if (dropConfiguration.ConditionMinLevel < level)
            {
                if ( ;
            }
        }*/
    }
}
